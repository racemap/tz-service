name: Build

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v1

      - name: build
        run: |
          docker build --target app -t "racemap/tz-service:${{env.GITHUB_RUN_NUMBER}}" .
